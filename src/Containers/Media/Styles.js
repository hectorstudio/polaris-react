import styled from 'styled-components';

const LibraryListWrap = styled.section`
    width: 100%;
    display: flex;
    flex-flow: row wrap;
    padding:3rem 2rem; 
`;

export default LibraryListWrap;
