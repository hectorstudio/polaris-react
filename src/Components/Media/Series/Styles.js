import styled from 'styled-components';

const SeasonsWrap = styled.div`
  margin-left:-1rem;
  float:left;
  width: calc(100% + 2rem);
  display:flex;
  flex-flow: row wrap;
`;

export default SeasonsWrap;
